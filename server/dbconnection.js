/* const mysql = require('mysql');
const connection = mysql.createPool({
host: 'localhost',
port: 3306,
user: 'root',
password: 'mjseo',
database: 'study'
});

module.exports=connection;
 */

module.exports = {
    host     : 'localhost',
    user     : 'root',
    password : 'mjseo',
    port     : 3306,
    database : 'study'
  };